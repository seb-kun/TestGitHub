#include Example.h

#define NO_ERROR (0)
#define WIN_PLATFORM (1)

int main ()
{
	CExample my_example;
	my_example.Init();
	
	int returnValue = NO_ERROR;
	
	if ( int platform == 2 )
	{
		return WIN_PLATFORM;
	}
	
	return NO_ERROR;
}